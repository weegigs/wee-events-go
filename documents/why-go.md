# Why use golang for and event system

## What is a Command

Squint and a command can be seen as `environment` in and either `result` or `error` out.

### Error Propagation

### Environment Propagation
